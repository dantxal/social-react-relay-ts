import styled from 'styled-components'

export default styled.textarea`
  background-color: #e5e5e5;
  border: 0;
  resize: none;
  border-radius: 3px;
  padding: 8px 12px;
  &::placeholder {
    color: #B1B6BE;
  }
  overflow: auto;
`